# =============================================================================
# ENVIRONMENT CONFIGURATIE VOORBEELD
# =============================================================================
# Kopieer dit bestand naar:
# - .env.development (voor lokale development)
# - .env.production (voor productie builds)
# 
# OF gebruik de bestaande .env.development en .env.production bestanden
# =============================================================================

# === BACKEND API ===
# Development: http://localhost:8082 (lokale Docker backend)
# Production:  https://dklemailservice.onrender.com
# BELANGRIJK: Zonder /api want de AuthProvider voegt deze al toe!
VITE_API_BASE_URL=http://localhost:8082
VITE_WS_URL=ws://localhost:8082/api/chat/ws
VITE_EMAIL_API_URL=http://localhost:8082
VITE_ENV=development

# === FRONTEND ===
# Development: http://localhost:5173 (Vite dev server)
# Production:  https://admin.dekoninklijkeloop.nl
VITE_APP_URL=http://localhost:5173

# === CLOUDINARY CONFIGURATIE ===
# Vul in met je Cloudinary credentials
VITE_CLOUDINARY_API_KEY=your_cloudinary_api_key
VITE_CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
VITE_CLOUDINARY_UPLOAD_PRESET=your_cloudinary_upload_preset

# === SUPABASE CONFIGURATIE (indien nog gebruikt) ===
VITE_SUPABASE_URL=https://your-supabase-url.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# === OPTIONELE CONFIGURATIE ===
VITE_API_TIMEOUT=30000
VITE_DEBUG=true

# =============================================================================
# LOKALE DEVELOPMENT SETUP
# =============================================================================
# 
# 1. BACKEND STARTEN:
#    In de backend directory (dklemailservice):
#    docker-compose -f docker-compose.dev.yml up -d
# 
# 2. VERIFIEER BACKEND:
#    curl http://localhost:8082/api/health
#    Verwacht: {"status":"healthy",...}
# 
# 3. FRONTEND STARTEN:
#    npm run dev
#    Frontend draait op: http://localhost:5173
# 
# 4. TESTEN:
#    Login credentials (lokaal):
#    - Email: admin@dekoninklijkeloop.nl
#    - Password: [check backend database]
# 
# =============================================================================
# PRODUCTIE BUILD
# =============================================================================
# 
# 1. Update .env.production met productie credentials
# 2. Build: npm run build
# 3. Deploy naar hosting platform
# 
# BELANGRIJK:
# - .env.development en .env.production zijn al geconfigureerd
# - Deze bestanden staan in .gitignore (niet in git)
# - Vul echte credentials in voor je gebruik
# 
# =============================================================================
# BACKEND API ENDPOINTS
# =============================================================================
# 
# PUBLIEK (geen auth):
# - POST /api/auth/login
# - GET  /api/health
# - GET  /api/albums
# - GET  /api/videos
# - GET  /api/sponsors
# 
# ADMIN (JWT vereist):
# - GET  /api/contact
# - GET  /api/aanmelding
# - GET  /api/users
# - GET  /api/newsletter
# - GET  /api/albums/admin
# - POST /api/photos
# - POST /api/videos
# - POST /api/sponsors
# - POST /api/steps/:id
# 
# Zie Backendinfo/FRONTEND_SETUP_QUICK.md voor meer details
# =============================================================================